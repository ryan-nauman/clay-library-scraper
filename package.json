{
  "name": "library",
  "version": "0.0.1",
  "description": "Scrape clay county library items on loan",
  "author": "ryanjnauman@gmail.com",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "dotenv": "^8.2.0",
    "puppeteer": "^5.5.0"
  },
  "devDependencies": {
    "@types/puppeteer": "^5.4.0",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "build": "tsc --outDir dist",
    "start": "yarn build && node dist/index.js",
    "debug": "yarn build && node --inspect-brk dist/index.js"
  }
}
